# Eval

Make technical decisions with build vs. buy analysis using Architecture Agent.

## Usage
```
/eval Should we build or buy a BI assistant?
/eval REST vs GraphQL for our API
/eval Which queue service for async processing?
/eval Generic PDF reading - Custom OCR build vs. use frontier multi-modal LLM?
```

Applies `architecture-agent/RULE.md` rules automatically.
